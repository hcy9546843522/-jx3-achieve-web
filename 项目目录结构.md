# 剑网3 成就看板 — 项目目录结构与说明

**项目路径**: `jx3-achieve-web/`  
**部署平台**: Vercel (Serverless)  
**技术栈**: FastAPI + 腾讯云 CloudBase (NoSQL) + 前端 HTML/JS/CSS 嵌入式  
**架构**: Monolith-Lite（单文件后端，所有前端资源内嵌在 Python 中）

---

## 目录树

```
jx3-achieve-web/
│
├── .gitignore                       # Git 忽略规则（__pycache__、.vercel、.env）
├── requirements.txt                 # Python 依赖清单
├── vercel.json                      # Vercel 部署配置（全部路由重写 → /api/index）
├── local_run.py                     # 本地开发启动脚本
│
├── 项目文档.md                      # 项目说明文档（架构、数据流、已知深坑）
├── 数据库相关信息.md                # CloudBase 数据库集合结构与 API 调用说明
├── 环境变量.txt                     # Vercel 部署所需的环境变量
│
├── jx3_all_backup.json.gz           # 全量成就数据库（压缩包）
│
└── api/
    ├── __pycache__/                 # Python 缓存（Git 忽略）
    └── index.py                     # ★ 唯一核心代码文件（~1100+ 行）
```

---

## 各文件详细说明

### 根目录

| 文件 | 说明 |
|------|------|
| `requirements.txt` | 依赖：`fastapi`(Web框架) `python-multipart`(表单) `uvicorn`(ASGI服务器) `requests`(HTTP客户端) |
| `vercel.json` | 单条路由规则：`"source": "/(.*)"` → `"destination": "/api/index"`，所有请求转发至 FastAPI 应用 |
| `local_run.py` | 本地启动：`uvicorn.run("api.index:app", host="0.0.0.0", port=8000, reload=True)` |
| `jx3_all_backup.json.gz` | **核心数据文件** — 全量成就数据库的 gzip 压缩包。包含所有成就的 ID、分类层级（一级/二级菜单）、场景归属、层数、攻略文本等。看板生成时解压后做 ID 匹配 |
| `.gitignore` | 忽略 `__pycache__/` `*.pyc` `.vercel` `.env` |
| `项目文档.md` | 项目架构说明（Monolith-Lite 架构）、数据流图、数据库集合说明、已知开发深坑与约束 |
| `数据库相关信息.md` | CloudBase 各集合（`html_cache` `query_logs` `user_notes`）的字段结构、权限策略、API 调用示例 |
| `环境变量.txt` | 3 个 Vercel 环境变量：`TCB_ENV_ID`(云环境ID) `TCB_API_KEY`(服务端 API 凭证) `ADMIN_EMAILS`(管理员邮箱白名单) |

### api/ — 唯一后端模块

| 文件 | 说明 |
|------|------|
| `__pycache__/` | Python 字节码缓存，Git 忽略 |
| `index.py` | **整个应用的唯一代码文件**（约 1100+ 行），内嵌了全部后端逻辑+前端HTML/CSS/JS。包含： |

**index.py 内功能模块划分：**

```
┌─ FastAPI 应用初始化 ─────────────────────────────┐
│  app = FastAPI()                                    │
│  BASE_DIR / LOCAL_JSON_FILE 路径解析                │
│  CloudBase API 地址 & 请求头配置                     │
│  管理员邮箱白名单解析                                 │
├─ 首页路由 GET / ──────────────────────────────────┤
│  登录/注册页 + UID 备注管理 + 成就生成入口            │
├─ 账户 API ────────────────────────────────────────┤
│  POST /register   → 注册（SHA256 密码哈希）          │
│  POST /login      → 登录鉴权                        │
│  POST /save_ids   → 保存/更新 JX3ID 列表             │
│  GET  /get_ids    → 查询某账号的 JX3ID 列表           │
│  DELETE /delete_id → 删除指定 JX3ID                  │
├─ 核心业务 POST /generate ─────────────────────────┤
│  ① 接收用户提交的 JX3ID 列表                         │
│  ② 调用剑网3官方 API 拉取每个 ID 的实时成就数据        │
│  ③ 解压 jx3_all_backup.json.gz 全量成就字典          │
│  ④ 逐成就比对 → 统计每个账号在各分类的完成进度         │
│  ⑤ 组装 HTML → 存入 CloudBase html_cache             │
│  ⑥ 返回跳转 /board/{doc_id}                         │
├─ 看板页 GET /board/{doc_id} ──────────────────────┤
│  从 html_cache 读取缓存的看板数据并渲染               │
├─ 嵌入的前端资源 ──────────────────────────────────┤
│  HTML 模板：成就卡片布局、进度条、筛选按钮             │
│  CSS 样式：卡片布局、配色方案、响应式                 │
│  JS 逻辑：按场景/层数/等级筛选、进度统计更新、脚本展开  │
├─ 管理后台 GET /admin/logs ────────────────────────┤
│  管理员专属：查看 query_logs 查询记录                 │
└────────────────────────────────────────────────────┘
```

---

## 核心业务流程图

```
用户提交 JX3ID（如: 12345,67890）
         │
         ▼
 ┌─────────────────────┐
 │  POST /generate      │
 │  ① 调用 JX3Box API  │──── 拉取实时成就数据
 │  ② 读取本地 json.gz │──── 加载全量成就库
 │  ③ 逐成就比对统计   │──── 各分类完成进度
 │  ④ 组装看板 HTML    │──── 嵌入式前端
 │  ⑤ 缓存到 CloudBase │──── html_cache 集合
 └─────────┬───────────┘
           ▼
    302 跳转 /board/{doc_id}
           ▼
 ┌─────────────────────┐
 │  GET /board/{id}     │
 │  → 读取 html_cache   │
 │  → 返回完整看板页面  │
 │  → 前端筛选/交互就绪 │
 └─────────────────────┘
```

---

## 数据库（CloudBase NoSQL）

| 集合 | 用途 | 权限 |
|------|------|------|
| `html_cache` | 缓存看板 HTML 数据（防刷新丢失） | ADMINONLY（服务端 API Key） |
| `query_logs` | 记录每次查询的 UID 组合和时间 | ADMINONLY（服务端 API Key） |
| `users` | 账户邮箱 + SHA256 密码哈希 | 服务端 API Key |
| `user_jx3ids` | 邮箱 → JX3ID 映射 + 自定义备注名 | 服务端 API Key |

---

## 已知约束（给 AI 看的关键信息）

1. **单文件红线**：严禁拆分 `index.py` — 保持单文件以适应大模型上下文窗口
2. **Vercel 10秒超时**：第三方 API 调用必须极速，单次查询 ≤ 5 个 ID
3. **CloudBase 多条件查询 BUG**：`where` 参数常失效，须在 Python 侧二次遍历比对
4. **EJSON 格式污染**：数字类型可能被包裹为 `{"$numberInt":"123"}`，需兼容清洗
5. **日志容错**：`query_logs` 写入必须设超时 1.5s 且 try-except 兜底
6. **BOM 陷阱**：文本写入要用二进制模式 `wb`，防止 UTF-8 BOM 导致 Vercel 部署报错
