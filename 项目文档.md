# 📁 项目文档 — 剑网3多角色成就看板 (jx3-achieve-web) V2

## 项目概览 (Overview)
- **Project**: 剑网3多角色成就看板(JX3 Achieve Web) - V2持久化版
- **Objective**: 提供高效的多账号角色成就与资历比对工具。支持数据缓存（防刷新丢失）、用户系统（保存UID及自定义角色备注）以及后台查询日志监控。
- **Tech_Route**: Python 3.9+, FastAPI, Vercel Serverless, 腾讯云 CloudBase (HTTP REST API), 原生 HTML/JS/CSS.

## 架构与数据流 (Architecture)
本项目采用 **“大模型友好型”极简单体（Monolith-Lite）架构**，核心逻辑高度集中。
1. **前端交互**: 纯原生 JS/CSS 配合 HTML 模板，实现滚动条位置记忆、跨层级全局搜索。
2. **后端服务**: Vercel Serverless (FastAPI) 接收表单与请求。
3. **数据组装**: 
   - 调用外部 JX3Box API 获取玩家实时成就进度。
   - 读取并解压本地字典 `jx3_all_backup.json.gz` 进行 ID 匹配。
   - 提取 CloudBase 数据库中的用户备注（若存在），替换原本的 UID 数字。
4. **视图渲染**: Python 后端动态生成并拼接包含全量 DOM 节点与样式脚本的 HTML 单页应用（SPA）返回给前端。

## 数据库与集合 (Database - Tencent CloudBase)
采用腾讯云 CloudBase (NoSQL) 文档型数据库，通过 `requests` 发起 HTTP API 调用，不使用任何官方 SDK。
* **`users`**: 存储账户信息（`email`, `password` 的 SHA256 哈希值）。
* **`user_jx3ids`**: 存储账户与 JX3ID 的映射及自定义备注名（`email`, `jx3_uid`, `alias_name`）。
* **`html_cache`**: 看板数据缓存库。存储结构化 `raw_data`（含成就比对结果），生成后跳转至 `/board/{doc_id}` 渲染，避免刷新页面导致数据丢失或重复消耗 API 额度。
* **`query_logs`**: 查询行为日志。记录每次生成的 UID 组合及时间戳（`query_uids`, `created_at`）。

## 部署与环境变量 (Deployment & Env)
* **平台**: Vercel (Hobby)
* **域名**: 阿里云独立域名 `jx3achieve.cloud`
* **DNS**: A(`@`→`216.198.79.1`) + CNAME(`www`→`eb6f23f81e4df3d8.vercel-dns-017.com`)
* **环境变量 (需在 Vercel 中配置)**:
    * `TCB_ENV_ID`: 腾讯云环境 ID。
    * `TCB_API_KEY`: 腾讯云服务端 API 凭证。
    * `ADMIN_EMAILS`: 管理员白名单邮箱（用于限制 `/admin/logs` 的访问权限，多个邮箱用逗号分隔）。

## 核心文件目录 (Data & Files)
- `api/index.py`: 绝对核心：包含所有后端路由、数据库交互、UI 模板与 JS 脚本
- `jx3_all_backup.json.gz`: 剑网3全量成就数据字典压缩包
- `requirements.txt`: 依赖清单 (fastapi, requests, python-multipart, uvicorn 等)
- `vercel.json`: Vercel 路由重写配置
- `local_run.py`: 本地 Uvicorn 启动测试脚本

## 已知深坑与开发约束 (Pitfalls & Constraints)
后续开发必须严格遵守以下规范，避免触发系统崩溃：
1. **架构限制**: **绝对禁止**随意拆分 `index.py` 文件或引入外部模板引擎（如 Jinja2）。保持单文件状态以适应大模型上下文窗口。
2. **Vercel 超时红线**: 免流版存在 10 秒强制中断限制。第三方请求、日志入库、数据组装必须极速。UI 需明确提示单次查询限制 ≤ 5 个 ID。
3. **CloudBase 多条件查询缺陷**: TCB 的 HTTP GET `where` 参数极易失效（尤其是多条件匹配时）。所有鉴权、查重操作，**必须在拉取列表后，于 Python 侧执行二次精确遍历比对**（例如：`if d.get("email") == email:`）。
4. **CloudBase EJSON 格式污染**: TCB 提取数字（如时间戳、成就数值）时，常自动包裹为 `{"$numberInt": "123"}` 格式。解析时必须编写针对 `dict` 类型的提取逻辑进行兼容清洗，防止程序抛出类型错误。
5. **日志入库容错**: 对 `query_logs` 集合的请求必须设定极短的超时（如 `timeout=1.5`）并被 `try-except` 包裹，绝不可因日志记录失败阻塞核心渲染业务。