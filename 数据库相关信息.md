以下是为编程 AI 准备的 CloudBase 数据库配置摘要，可直接复制粘贴。

---

### 数据库类型
腾讯云 CloudBase **文档型数据库（NoSQL）**，兼容 MongoDB 语法，通过 HTTP API 访问。

### 连接与认证
- **基础 URL**: `https://{envId}.api.tcloudbasegateway.com`
- **实例 ID**: `(default)`  （免费版固定值，含括号）
- **数据库名**: `(default)`  （免费版固定值，含括号）
- **认证方式**: `Authorization: Bearer <服务端 API Key>`
  - **⚠️ 服务端 API Key 仅用于后端（如 Vercel 上的 FastAPI），严禁暴露到前端代码**
  - **前端（浏览器）若需直接操作 `user_notes` 集合，必须使用 CloudBase Web SDK + 匿名登录/微信登录，并用客户端的 Publishable Key 初始化**

### 环境变量（已配置于 Vercel）
| 变量名 | 值 |
|-------|-----|
| `TCB_ENV_ID` | 你的环境 ID（形如 `jx3-achieve-web-d0fk9vwj07c43747`） |
| `TCB_API_KEY` | 你的服务端 API Key（仅后端使用） |

---

### API 端点与已测试通过的操作

#### 1. 插入文档
- **方法**: `POST`
- **路径**: `/v1/database/instances/(default)/databases/(default)/collections/{collection}/documents`
- **请求体格式**（已验证）:
```json
{
  "data": [
    {
      "字段1": "值1",
      "字段2": "值2"
    }
  ]
}
```
- **成功响应**: HTTP 200，返回包含自动生成 `_id` 的数据

#### 2. 查询文档
- **方法**: `GET`
- **路径**: 同上
- **查询参数**: `?limit=100`
- **成功响应**:
```json
{
  "offset": 0,
  "limit": 20,
  "list": [ ... ]
}
```

#### 3. 更新文档
- **方法**: `PATCH`
- **路径**: `/v1/database/instances/(default)/databases/(default)/collections/{collection}/documents/{doc_id}`
- **请求体**:
```json
{
  "data": {
    "要更新的字段": "新值"
  }
}
```

---

### 集合清单与权限策略

| 集合名 | 用途 | 权限 | 访问方式 |
|--------|------|------|----------|
| `html_cache` | 缓存 HTML 页面 | `ADMINONLY` | 仅通过服务端 API Key 读写 |
| `query_logs` | 查询日志记录 | `ADMINONLY` | 仅通过服务端 API Key 读写 |
| `user_notes` | 用户个人备注（增删改查） | `PRIVATE` | **客户端通过 CloudBase Web SDK 操作**（用户只能读写自己的数据，通过 `_openid` 隔离）；后端若需批量处理也可用 API Key |

> **`user_notes` 特别说明**：
> - 前端调用时，必须使用 CloudBase Web SDK 初始化，并让用户完成登录（匿名登录或微信登录均可）。
> - 每条文档会自动注入 `_openid`，权限 `PRIVATE` 会确保用户只能操作自己创建的文档。
> - 若后端用 API Key 操作该集合，会绕过权限限制，请谨慎。

---

### 套餐与环境信息
- 当前套餐：体验版（免费）
- 免费额度：存储 2GB、读写各 100 万次/月
- 到期日：2026年11月2日（届时需迁移至个人版长期免费环境）
- 数据库备份：**建议在控制台开启“数据备份与回档”功能**

---

### 代码调用示例（后端 Python）
```python
import os
import httpx

ENV_ID = os.environ["TCB_ENV_ID"]
API_KEY = os.environ["TCB_API_KEY"]
BASE = f"https://{ENV_ID}.api.tcloudbasegateway.com"
PATH = "/v1/database/instances/(default)/databases/(default)/collections"

headers = {
    "Authorization": f"Bearer {API_KEY}",
    "Content-Type": "application/json; charset=utf-8"
}

async def insert_html_cache(url: str, html: str):
    async with httpx.AsyncClient() as client:
        resp = await client.post(
            f"{BASE}{PATH}/html_cache/documents",
            json={"data": [{"url": url, "html": html, "timestamp": "..."}]},
            headers=headers
        )
        resp.raise_for_status()
        return resp.json()
```

---

### 前端 SDK 调用 `user_notes` 概要（供前端 AI 参考）
```javascript
import cloudbase from '@cloudbase/js-sdk';
const app = cloudbase.init({
  env: '你的环境ID'  // 只能暴露 Publishable Key，前端不用服务端 API Key
});
const auth = app.auth();
await auth.anonymousAuthProvider().signIn(); // 匿名登录
const db = app.database();
const result = await db.collection('user_notes').add({ note: '...' });
```

---

以上就是数据库侧的全部必要信息，无遗漏。可直接提供给编程 AI。